# Copy this to .env and adjust if your backend URL is different.
# Vite requires the VITE_ prefix for env vars to be exposed to the browser.
VITE_API_BASE=https://lead-ledger-backend.onrender.com/api
